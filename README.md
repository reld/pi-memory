# pi-memory

Pi package for per-project long-term memory.

## Planned structure

- TypeScript Pi extension in `src/extension/`
- thin Pi entrypoint in `extensions/pi-memory.ts`
- Go backend in `go/`
- packaged binaries in `resources/bin/`

This repository is currently in early scaffolding.
